<?xml version="1.0"?>
<ruleset name="Cooked Coding Standards">
	<description>PHPCS configuration for Cooked</description>

	<file>.</file>

    <!-- Exclude files -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>/wp-admin/*</exclude-pattern>
	<exclude-pattern>/wp-includes/*</exclude-pattern>
	<exclude-pattern>/wp-*.php</exclude-pattern>
	<exclude-pattern>/index.php</exclude-pattern>
	<exclude-pattern>/xmlrpc.php</exclude-pattern>
	<exclude-pattern>/wp-content/plugins/*</exclude-pattern>

    <!-- Only check PHP files. -->
	<arg name="extensions" value="php" />

	<!-- Show progress, show the error codes for each message (source). -->
	<arg value="ps" />

    <!-- Add colors -->
    <arg name="colors"/>

	<!-- Check up to 8 files simultaneously. -->
	<arg name="parallel" value="8"/>

	<!-- Configs -->
	<config name="minimum_supported_wp_version" value="6.0" />
	<config name="testVersion" value="7.4-" />

    <!-- Rules -->
	<rule ref="WordPress">
		<exclude name="Generic.WhiteSpace.DisallowSpaceIndent.SpacesUsed"/>
		<exclude name="Universal.Arrays.DisallowShortArraySyntax.Found" />
	</rule>

	<!-- Include the WordPress-Extra standard. -->
	<rule ref="WordPress-Extra">
		<!--
		We may want a middle ground though. The best way to do this is add the
		entire ruleset, then rule by rule, remove ones that don't suit a project.
		We can do this by running `phpcs` with the '-s' flag, which allows us to
		see the names of the sniffs reporting errors.
		Once we know the sniff names, we can opt to exclude sniffs which don't
		suit our project like so.

		The below two examples just show how you can exclude rules/error codes.
		They are not intended as advice about which sniffs to exclude.
		-->

		<!--
		<exclude name="WordPress.WhiteSpace.ControlStructureSpacing"/>
		<exclude name="Modernize.FunctionCalls.Dirname.Nested"/>
		-->
        <exclude name="PEAR.Files.IncludingFile.UseRequire" />
	</rule>

	<!-- Let's also check that everything is properly documented. -->
	<rule ref="WordPress-Docs"/>

	<!-- Add in some extra rules from other standards. -->
	<rule ref="Generic.Commenting.Todo"/>

	<rule ref="PHPCompatibility"/>

	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>cooked.php</exclude-pattern>
		<exclude-pattern>/includes/</exclude-pattern>
	</rule>

	<rule ref="WordPress.Files.FileName.NotHyphenatedLowercase">
		<exclude-pattern>/includes/</exclude-pattern>
	</rule>

	<rule ref="WordPress.NamingConventions.ValidVariableName.VariableNotSnakeCase">
		<exclude-pattern>cooked.php</exclude-pattern>
	</rule>

	<rule ref="WordPress.DB.DirectDatabaseQuery">
    <exclude-pattern>*.php</exclude-pattern>
	</rule>

	<rule ref="WordPress.WP.GlobalVariablesOverride">
		<exclude-pattern>*.php</exclude-pattern>
	</rule>

    <rule ref="Squiz.Commenting.FileComment">
        <exclude-pattern>includes/</exclude-pattern>
    </rule>

	<!-- Custom capabilities -->
	<rule ref="WordPress.WP.Capabilities">
		<properties>
			<property name="custom_capabilities" type="array">
				<element key="0" value="edit_cooked_recipes"/>
				<element key="1" value="edit_cooked_settings"/>
				<element key="2" value="edit_cooked_default_template"/>
			</property>
		</properties>
	</rule>

	<!-- Text Domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="cooked"/>
			</property>
		</properties>
	</rule>

</ruleset>
